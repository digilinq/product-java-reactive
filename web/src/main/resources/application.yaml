spring:
  data:
    mongodb:
      database:
      uri:
      auto-index-creation: true
logging:
  level:
    root: INFO

---
spring:
  config:
    activate:
      on-profile: local
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: products-java
